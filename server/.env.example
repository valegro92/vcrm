# Database Configuration

# Option 1: PostgreSQL (recommended for production)
# Use full connection string
DATABASE_URL=postgresql://username:password@host:port/database_name

# Option 2: PostgreSQL with individual variables
# DB_HOST=localhost
# DB_PORT=5432
# DB_NAME=vcrm_db
# DB_USER=postgres
# DB_PASSWORD=your_password

# Option 3: SQLite (fallback for local development)
# Leave DATABASE_URL empty and SQLite will be used
# DB_PATH=/data/crm.db  # Optional: custom SQLite database path

# Server Configuration
PORT=8080
NODE_ENV=development

# JWT Secret (CHANGE THIS IN PRODUCTION!)
JWT_SECRET=your-secret-key-change-this-in-production

# Optional: Additional CORS origins (comma-separated)
# ALLOWED_ORIGINS=https://example.com,https://another.com
